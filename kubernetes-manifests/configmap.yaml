apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: react-kub-app
  labels:
    app: react-kub-app
data:
  # Backend configuration
  BACKEND_PORT: "5000"
  BACKEND_HOST: "0.0.0.0"
  
  # Frontend configuration
  # Note: For frontend, VITE_API_BASE needs to be set at build time
  # Use environment-specific values in deployment
